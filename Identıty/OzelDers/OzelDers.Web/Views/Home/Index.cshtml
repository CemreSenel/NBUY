@using OzelDers.Web.Models.Dtos
@model List<UserDto>

<div class="text-center">
    <h1 class="display-6">Welcome OzelDers</h1>
    <div class="mt-4">
        <div class="row">
            @foreach (var userDto in Model)
            {
                <div class="col-md-4">
                    @await Html.PartialAsync("_UserPartial", userDto)
                </div>
            }
        </div>
    </div>
</div>
